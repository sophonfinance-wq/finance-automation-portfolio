# Platform-wide dependencies for the finance automation portfolio.
# Every system in this repo runs on stdlib + these two packages only.
# (No pandas / numpy / faker — synthetic data is generated with stdlib random.)

openpyxl>=3.1
pytest>=8.0
