# Platform-wide dependencies for the finance automation portfolio.
# Every system runs on the standard library + these two packages only.
# (No pandas / numpy / faker — synthetic data is generated with stdlib random.)
#
# Pinned for reproducibility: these are the exact versions CI runs and the
# suite is verified against. Bump deliberately, re-run `pytest`, then commit.
openpyxl==3.1.5
pytest==9.1.0
