{
  "name": "sophie-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that proxies the Sophon Finance site assistant (Sophie) to the Anthropic API, holding the API key as a secret.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
