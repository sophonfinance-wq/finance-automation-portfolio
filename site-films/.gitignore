why/work/
why/work_urls.json

# Raw b-roll sources are large and re-fetchable from broll/manifest.json,
# which records the generation job id for every clip. Only web-optimised
# derivatives belong in docs/assets.
broll/*.mp4
