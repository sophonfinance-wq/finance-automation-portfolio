# Working files: source renders, poster PNGs, rejected takes, verdicts.
# Every clip re-downloads from its job id in manifest.json, so none of this
# needs to be in the repo.
raw-*.mp4
rejected-*.mp4
stab-*.mp4
engine-*.png
*-*.png
verdicts.json
